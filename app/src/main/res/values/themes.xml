<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BhagavadGita" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="MyAppBarStyle" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:background">#FFEB3B</item>
        <item name="titleTextColor">@color/black</item>
        <item name="subtitleTextColor">@color/black</item>
        <item name="navigationIconTint">#FF9800</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="elevation">4dp</item>
    </style>
    <style name="Theme.BhagavadGita" parent="Base.Theme.BhagavadGita" />
</resources>